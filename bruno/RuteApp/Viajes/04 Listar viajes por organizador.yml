info:
  name: Listar viajes por organizador
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/viajes/organizador/{{organizadorId}}"
  auth:
    type: bearer
    token: "{{usuarioToken}}"
