info:
  name: BUSCAR PUERTO VALLARTA GEOAPIFY
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/lugares/autocompletar?texto=Puerto"
  params:
    - name: texto
      value: Puerto
      type: query
  auth:
    type: bearer
    token: "{{usuarioToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
