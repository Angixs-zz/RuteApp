info:
  name: Iniciar sesion como agencia
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/auth/login"
  body:
    type: json
    data: |-
      {
        "correo": "{{agenciaCorreo}}",
        "password": "{{agenciaPassword}}"
      }
  auth: inherit
