info:
  name: Listar por organizador
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/viajes/organizador/{{organizadorId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
