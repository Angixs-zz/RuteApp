info:
  name: Listar Roles Usuario
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/roles"
  auth:
    type: bearer
    token: "{{usuarioToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
