info:
  name: Buscar usuario por id
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/usuarios/{{usuarioId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
