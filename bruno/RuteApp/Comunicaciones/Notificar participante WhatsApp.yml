info:
  name: Notificar participante WhatsApp
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/participantes/{{participanteId}}/notificar-whatsapp"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
