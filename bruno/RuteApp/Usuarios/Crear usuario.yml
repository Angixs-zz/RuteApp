info:
  name: Crear usuario
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/usuarios"
  body:
    type: json
    data: |-
      {
        "nombre": "Santi",
        "correo": "hombrearaña@gmail.com",
        "password": "Miguel123!",
        "avatar": null,
        "rolId": {{rolId}}
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
