info:
  name: Listar por pagador
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/gastos/pagador/{{pagadorId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
