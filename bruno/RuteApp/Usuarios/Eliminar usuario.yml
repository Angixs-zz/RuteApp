info:
  name: Eliminar usuario
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/api/usuarios/{{usuarioId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
