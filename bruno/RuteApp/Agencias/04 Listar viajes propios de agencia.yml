info:
  name: Listar viajes propios de agencia
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/viajes/organizador/{{agenciaId}}"
  auth:
    type: bearer
    token: "{{agenciaToken}}"
