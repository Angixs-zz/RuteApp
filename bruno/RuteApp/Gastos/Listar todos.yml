info:
  name: Listar todos
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/gastos"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
