info:
  name: Listar Roles Admin
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/roles"
  auth:
    type: bearer
    token: "{{adminToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
