info:
  name: Buscar VIaje por ID Inexistente
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/api/viajes/99999"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
