info:
  name: Listar gastos realizados por un pagador
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/gastos/pagador/{{pagadorId}}"
  auth:
    type: bearer
    token: "{{usuarioToken}}"
