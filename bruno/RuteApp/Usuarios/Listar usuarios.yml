info:
  name: Listar usuarios
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/usuarios"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
