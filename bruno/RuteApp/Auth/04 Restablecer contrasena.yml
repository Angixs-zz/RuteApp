info:
  name: Restablecer contrasena con token
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/api/auth/recuperacion/restablecer"
  body:
    type: json
    data: |-
      {
        "token": "{{recoveryToken}}",
        "password": "NuevaClave123!"
      }
  auth: inherit
