info:
  name: Buscar Gast por ID
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/api/gastos/99999"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
