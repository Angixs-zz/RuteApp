info:
  name: LISTAR VIAJES PAGINADOS
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/api/viajes?page=0&size=2"
  params:
    - name: page
      value: "0"
      type: query
    - name: size
      value: "2"
      type: query
  auth:
    type: bearer
    token: "{{usuarioToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
