info:
  name: Eliminar participante por ID
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{baseUrl}}/api/participantes/{{participanteId}}"
  auth:
    type: bearer
    token: "{{usuarioToken}}"
