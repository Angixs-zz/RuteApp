info:
  name: Listar participantes de un viaje
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/participantes/viaje/{{viajeId}}"
  auth:
    type: bearer
    token: "{{usuarioToken}}"
