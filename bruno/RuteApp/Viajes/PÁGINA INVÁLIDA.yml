info:
  name: PÁGINA INVÁLIDA
  type: http
  seq: 13

http:
  method: GET
  url: "{{baseUrl}}/api/viajes?page=-1&size=10"
  auth:
    type: bearer
    token: "{{usuarioToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
