info:
  name: Buscar por ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/gastos/{{gastoId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
