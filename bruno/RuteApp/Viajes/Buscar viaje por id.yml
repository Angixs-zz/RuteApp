info:
  name: Buscar viaje por id
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/viajes/{{viajeId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
