info:
  name: Eliminar viaje
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseUrl}}/api/viajes/{{viajeId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
