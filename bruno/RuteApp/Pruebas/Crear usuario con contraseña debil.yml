info:
  name: Crear usuario con contraseña debil
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/usuarios"
  body:
    type: json
    data: |-
      {
        "nombre": "Miguel Angel Prueba",
        "correo": "hola@gmail.com",
        "password": "1234",
        "avatar": null,
        "rolId": {{rolId}}
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
