info:
  name: Bloquear gastos internos a usuario ajeno
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/gastos/viaje/{{agenciaViajeId}}"
  auth:
    type: bearer
    token: "{{usuarioToken}}"
