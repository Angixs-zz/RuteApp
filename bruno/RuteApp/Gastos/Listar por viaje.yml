info:
  name: Listar por viaje
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/gastos/viaje/{{viajeId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
