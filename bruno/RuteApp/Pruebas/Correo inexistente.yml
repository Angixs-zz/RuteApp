info:
  name: Correo inexistente
  type: http
  seq: 11

http:
  method: POST
  url: "{{baseUrl}}/api/auth/login"
  body:
    type: json
    data: |-
      {
        "correo": "noexiste@ruteapp.com",
        "password": "Miguel123!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
