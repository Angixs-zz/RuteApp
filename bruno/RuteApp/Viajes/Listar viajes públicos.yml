info:
  name: Listar viajes públicos
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/viajes/publicos"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
