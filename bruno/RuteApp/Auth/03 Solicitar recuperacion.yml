info:
  name: Solicitar recuperacion de contrasena
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/auth/recuperacion/solicitar"
  body:
    type: json
    data: |-
      {
        "correo": "{{authCorreo}}"
      }
  auth: inherit
