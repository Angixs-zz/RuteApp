info:
  name: CREAR PARTICIPANTES
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/participantes"
  body:
    type: json
    data: |-
      {
        "usuarioId": 2,
        "viajeId": 1,
        "estadoInvitacion": "PENDIENTE",
        "permisoColaborar": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
