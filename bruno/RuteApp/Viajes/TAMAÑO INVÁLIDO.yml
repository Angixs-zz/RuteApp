info:
  name: TAMAÑO INVÁLIDO
  type: http
  seq: 14

http:
  method: GET
  url: "{{baseUrl}}/api/viajes?page=0&size=100"
  auth:
    type: bearer
    token: "{{usuarioToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
