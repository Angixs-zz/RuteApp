info:
  name: Eliminar gasto
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseUrl}}/api/gastos/{{gastoId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
