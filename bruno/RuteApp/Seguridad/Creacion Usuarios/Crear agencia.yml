info:
  name: Crear agencia
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/usuarios"
  body:
    type: json
    data: |-
      {
        "nombre": "Agencia Prueba",
        "correo": "agencia@ruteapp.com",
        "password": "Prueba123!",
        "avatar": null,
        "rolId": 3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
